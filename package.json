{
  "name": "swapme",
  "private": true,
  "version": "0.0.0",
  "description": "SwapMe projects website",
  "license": "Ruben Magnin",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0",
    "express": "4.x",
    "sqlite3": "3.x",
    "body-parser": "^1.15.2",
    "jasmine-core": "^2.4.1",
    "karma": "^0.13.22",
    "karma-chrome-launcher": "^0.2.3",
    "karma-jasmine": "^0.3.8",
    "protractor": "^3.2.2"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "start node restAPI.js",
    "start": "http-server ./app -a localhost -p 8001 -c-1",
    "pretest": "npm install",
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js --single-run",
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update",
    "preprotractor": "npm run update-webdriver",
    "protractor": "protractor ./app/e2e-tests/protractor.conf.js"
  }
}
